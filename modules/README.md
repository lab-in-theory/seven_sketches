## Using modules in Julia REPL

```
# be sure to add the directory with your helper modules to Julia's load path
# show current directory
> pwd() 

#push current directory to LOAD_PATH
> push!(LOAD_PATH, pwd())
``
